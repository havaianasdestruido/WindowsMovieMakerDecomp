# CLAUDE.md

This repository uses `AGENTS.md` as its single canonical instruction file.

Read `AGENTS.md` at the repository root and follow it as the project's
authoritative instructions. Do not treat this file as a separate source
of truth. Everything you need (build/test commands, conventions, security
rules, and gotchas) lives there.